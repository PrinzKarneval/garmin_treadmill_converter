# garmin_treadmill_converter
Takes an array of intervalls and rewrites the speeds and distances of the run.


laps = [
  (<DURATIONS_IN_SECONDS>, <SPPED_IN_KPH>),
  ...
]

Specify file name!
Execute!
